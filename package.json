{
  "name": "climb-spotter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "deploy:scraper-worker": "pnpm --filter scraper-worker run deploy"
  },
  "packageManager": "pnpm@9.5.0",
  "devDependencies": {
    "wrangler": "^4.8.0"
  }
}
